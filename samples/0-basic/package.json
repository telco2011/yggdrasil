{
  "name": "0-basic",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean": "rm -rf dist logs",
    "clean:all": "rm -rf dist logs node_modules",
    "compile": "npm run clean && tsc",
    "compile:test": "npm run clean && tsc -p tsconfig.spec.json",
    "start": "npm run compile && node dist/ignition.js",
    "test": "npm run compile:test && mocha --exit dist/test/**/*.spec.js"
  },
  "author": "David Lopez",
  "license": "MIT",
  "devDependencies": {
    "@yggdrasil/testing": "^0.1.0-alpha.1",
    "typescript": "^2.6.2"
  },
  "dependencies": {
    "@yggdrasil/logger": "^0.1.0-alpha.1",
    "@yggdrasil/mvc": "^0.1.0-alpha.1",
    "@yggdrasil/startup": "^0.1.0-alpha.1"
  }
}
