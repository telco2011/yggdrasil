import { suite, test } from '../../../testing';

import { expect, assert } from 'chai';

import { MonitorSessionCtrl } from './session.ctrl';

@suite('core.monitoring.controllers')
class MonitorSessionCtrlTestSuite {

  @test()
  public notYetImplemented(done) {
    assert.fail();
    done();
  }

}
